apiVersion: 1

datasources:
  - name: Postgres
    type: postgres
    url: postgres:5432
    database: postgres
    access: proxy
    user: postgres
    password: postgres
    basicAuth: true
    isDefault: true
    jsonData:
      sslmode: disable
    version: 12
    editable: false

